//Failed, I'll try again later

function nextHigherNumber(n) {}

console.log(nextHigherNumber(1234));
console.log(nextHigherNumber(13542));
console.log(nextHigherNumber(4321));

// Write a function to find the next higher number using the same digits of a given number.

// nstructions
// Consider the integer 13542. The next number that is higher than 13542 by rearranging its digits is 14235.

// Given an integer n, return the next higher number using exactly the same digits as in n.

// If the next higher number doesn't exist, return -1.

// Example
// For this input
// n = 1234

// the result should be:
// 1243

// Reason: The next higher number that can be formed by rearranging the digits of 1234 is 1243.

// Example
// For this input
// n = 4321

// the result should be:
// -1

// Reason: There is no possible rearrangement of the digits that can create a number larger than 4321. So our expected output is -1.
